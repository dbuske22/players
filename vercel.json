{
  "installCommand": "npm install -g bun && bun install && cd backend && bun install && cd ../frontend && bun install",
  "buildCommand": "cd frontend && bunx expo export --platform web",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.ts" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
