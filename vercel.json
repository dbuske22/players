{
  "installCommand": "npm install -g bun && bun install && cd backend && bun install && cd ../frontend && bun install",
  "buildCommand": "cd frontend && bunx expo export --platform web && cd .. && ./backend/node_modules/.bin/esbuild api/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=api/bundle.cjs --external:fsevents",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/bundle.cjs": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/bundle.cjs" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
